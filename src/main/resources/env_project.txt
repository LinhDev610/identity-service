BREVO_API_KEY=your_brevo_api_key_here;BREVO_EMAIL=your_email@gmail.com;DB_URL=jdbc:mysql://localhost:3306/identity_service;DBMS_PASSWORD=root;DBMS_USERNAME=root;JWT_SIGNERKEY=your_jwt_signer_key_here;SECRET_KEY=your_secret_key_here

build .jar and target:
1. mvn package -DskipTests
2. ./mvnw package -DskipTests

run: java -jar ./identity...

clean:
1. mvn clean
2. ./mvnw clean